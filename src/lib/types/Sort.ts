/**
 * Sort option types for blog posts
 */
export type SortOption = 'published_date' | 'view_count' | 'comment_count';

/**
 * Sort direction (currently only descending is used)
 */
export type SortDirection = 'asc' | 'desc';
